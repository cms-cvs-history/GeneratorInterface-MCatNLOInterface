process TEST =  {

	untracked PSet maxEvents = {untracked int32 output = 100}

	source = MCatNLOSource 
    	{ 
       		untracked int32 	herwigVerbosity 	= 0
       		untracked int32 	mcatnloVerbosity 	= 0
       		untracked bool 		herwigHepMCVerbosity 	= false
       		untracked int32 	maxEventsToPrint 	= 0

	       	untracked int32 	processNumber 		= -1361

       		untracked string 	stringFileName 		= "output/stringInput.txt"
		untracked bool 		doHardEvents 		= true
		untracked int32		numHardEvents		= 110
    		untracked string 	lhapdfSetPath 		= ""
		untracked bool		useJimmy		= true
		untracked bool		doMPInteraction		= true
		untracked bool 		printCards		= false

		# default input file
       		include "GeneratorInterface/MCatNLOInterface/test/mcatnloZee.cfi"
	}

    	service = RandomNumberGeneratorService {
	        untracked uint32 	sourceSeed 		= 235434
    	}

	module filter = MCatNLOFilter {}

	path p1 = {filter}

    	module out = PoolOutputModule { 
        	untracked string 	fileName = "output/zee_mcatnlo.root" 	
		untracked PSet SelectEvents = {
      			vstring SelectEvents = {"p1"}
		}
    	}

    	endpath p = { out }
}
